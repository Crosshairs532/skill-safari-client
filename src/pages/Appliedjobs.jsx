import { useLoaderData } from 'react-router-dom';

const Appliedjobs = () => {
    const load = useLoaderData();
    console.log(load);
    return (
        <div>

        </div>
    );
};

export default Appliedjobs;